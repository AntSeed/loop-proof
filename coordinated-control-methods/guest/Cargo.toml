[package]
name = "coordinated-control-guest"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
risc0-zkvm = { version = "3.0", default-features = false, features = ["std"] }
enforcement-core = { path = "../../enforcement-core" }
serde_json = "1"

[profile.release]
lto = "thin"
opt-level = 3
